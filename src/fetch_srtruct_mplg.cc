
#include <stdlib.h> 
#include <fstream> 
#include <sstream> 
#include <cstdio>
#include <cstdlib>

template <>

typedef struct n_alloc {
  struct n_alloc *next;
  char* buffer = new char[255];;
}

struct n_alloc *head;
extern char name[];


struct n_alloc *insert()

void alloc_poke_data(std::ostream& stream){
  stream << << 
}

{
  struct n_alloc *n_ptr = &head; 

  while (n_alloc->next){
    if(strcmp())
      break;
    
  }
}
/* 
*/ 
